import random

list = ["white", "orange", "orange and white"]

class Color(object):
    list = ["white", "orange", "orange and white"]
    def correct_answer():
        color = random.choice(list)
        return color
